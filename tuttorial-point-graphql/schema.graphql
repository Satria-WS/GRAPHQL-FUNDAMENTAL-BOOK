type Query {
    test: String
    students: [Students]
}

type Students {
    id: ID!
    firstName: String
    lastName: String
    email: String
    Password: String
    college: College
}

type College {
    id: ID!
    name: String
    rating: Float
}
